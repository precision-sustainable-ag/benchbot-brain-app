"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class sdk_reqs(object):

    __slots__ = ["init_sdk", "cam_conn", "cam_status", "cam_trigger", "cam_disconn", "release_sdk"]

    __typenames__ = ["boolean", "boolean", "boolean", "boolean", "boolean", "boolean"]

    __dimensions__ = [None, None, None, None, None, None]

    def __init__(self):
        self.init_sdk = False
        """ LCM Type: boolean """
        self.cam_conn = False
        """ LCM Type: boolean """
        self.cam_status = False
        """ LCM Type: boolean """
        self.cam_trigger = False
        """ LCM Type: boolean """
        self.cam_disconn = False
        """ LCM Type: boolean """
        self.release_sdk = False
        """ LCM Type: boolean """

    def encode(self):
        buf = BytesIO()
        buf.write(sdk_reqs._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">bbbbbb", self.init_sdk, self.cam_conn, self.cam_status, self.cam_trigger, self.cam_disconn, self.release_sdk))

    @staticmethod
    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != sdk_reqs._get_packed_fingerprint():
            raise ValueError("Decode error")
        return sdk_reqs._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = sdk_reqs()
        self.init_sdk = bool(struct.unpack('b', buf.read(1))[0])
        self.cam_conn = bool(struct.unpack('b', buf.read(1))[0])
        self.cam_status = bool(struct.unpack('b', buf.read(1))[0])
        self.cam_trigger = bool(struct.unpack('b', buf.read(1))[0])
        self.cam_disconn = bool(struct.unpack('b', buf.read(1))[0])
        self.release_sdk = bool(struct.unpack('b', buf.read(1))[0])
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if sdk_reqs in parents: return 0
        tmphash = (0x26b94e8cc0f7703) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if sdk_reqs._packed_fingerprint is None:
            sdk_reqs._packed_fingerprint = struct.pack(">Q", sdk_reqs._get_hash_recursive([]))
        return sdk_reqs._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", sdk_reqs._get_packed_fingerprint())[0]

